#)Spring mvc framework?
Spring mvc framework provides decoupled way of developing web applicaitons with simpole comcept like dispatcher servlet ,ModelAndView and View Resolver,it makes it easy to develop web applications.


#)Spring MVC (Model–View–Controller) is a framework in Spring used to build web applications.
It separates the application into three layers so the code becomes clean, maintainable, and testable.
