#)What is Spring Boot?
Spring Boot is a framework built on top of Spring that simplifies configuration 
1)using auto-configuration,
 2)starter dependencies, 
 3)and embedded servers.()


-------------------------------------------------------------------
 #)What is Auto Configuration?
Spring Boot automatically configures beans based on classpath.

------------------------------------------------------------

If you added spring-boot-starter-web → Spring Boot auto-configures Tomcat, DispatcherServlet, JSON converters, REST.
If you added spring-boot-starter-data-jpa → It auto-configures Hibernate, DataSource, EntityManager.
You don’t need to manually write configuration classes.


----------------------------------------------------------------




#)ResponseEnity
“ResponseEntity is used to send custom HTTP responses (status + headers + body) in a REST API.”

--------------------------------------------------------------------------------
Why do we use ResponseEntity? 
1️⃣ To return proper HTTP status codes

Ex: 200 OK, 201 CREATED, 404 NOT FOUND, 400 BAD REQUEST

2️⃣ To send custom headers

Example: tokens, metadata, pagination info

3️⃣ To return custom JSON body

Clean and consistent responses

4️⃣ Helps create RESTful APIs

REST requires correct response structure → ResponseEntity supports that


Example-----------------------------------
Example 1: Success response
@GetMapping("/user/{id}")
public ResponseEntity<User> getUser(@PathVariable int id) {
    User user = service.getUser(id);
    return ResponseEntity.ok(user); // 200 OK
}

Example 2: When a resource is created
@PostMapping("/user")
public ResponseEntity<String> saveUser(@RequestBody User user) {
    service.save(user);
    return ResponseEntity.status(HttpStatus.CREATED)
                         .body("User created successfully");
}

Example 3: When resource is not found (404)
@GetMapping("/product/{id}")
public ResponseEntity<?> getProduct(@PathVariable int id) {
    Product p = repo.findById(id).orElse(null);

    if (p == null) {
        return ResponseEntity.status(HttpStatus.NOT_FOUND)
                             .body("Product Not Found");
    }
    return ResponseEntity.ok(p);
}

------------------------




-------------------------------------------------------

#)What are the Spring boot starters?
or
#)What are Starter dependencies?
Starters are a collection of pre-configured depencies that makes
it easier to develop particular kinds of aoolications.
spring-boot-starter-web => web se related sare  configuraiton and dependecy rlhu jaise  hi use kiya sb jldi se downlaod ho jata hai.
#) These startes include all of the dependecies ,version control and configarion needed to amke certain features of a speing boot  applicaiton funcitonal.
#)spring-boot-starter-web is a starter used to build web applications.
It brings Spring MVC, embedded Tomcat, and JSON support out of the box.”
or
Instead of adding many separate dependencies, a starter gives you everything you need for a specific feature.
#)You include one dependency, and Spring Boot pulls all required libraries automatically.

if we not user starter we need to do manually everything
we need to add
(a)Add manual dependency 
b)Ensures compatible versions
c)Provides auto-configuration
(d) Reduces boilerplate configuration
-----------------------------------------------------------------------

What does it include?

When you add this in pom.xml:

<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-web</artifactId>
</dependency>


Spring Boot automatically includes:

✅ 1. Spring MVC

For handling web requests (controllers, endpoints)

✅ 2. Embedded Tomcat Server

Spring Boot’s default web server (runs on port 8080)

✅ 3. Jackson

For JSON serialization/deserialization
(Java objects ↔ JSON)

✅ 4. Validation API

For request validation (@Valid, @NotNull, etc.)

✅ What can you build with spring-boot-starter-web?

✅ REST APIs
✅ Web MVC apps (Thymeleaf/JSP)
✅ Microservices
✅ Backend services


spring-boot-starter-secuirty -Authentication and Authorization using spring secuirty.
spring-boot-starter-data-jpa->Spring Dataamework jpa with hubernate
spring-boot-starter-cache-->Enablinr spring framework  caching suport.

--------------------------------------------------------------------
#)What are the key dependecies of spring boot?
1)spring boot-starter-parent(provide defualt configuraiton for spring-based applicziton)
Provide the dfualt compiler leevel as java 1.8 and UTF-8 source
provide defualt config for maven plugis such as maven-surfire

2)maven-plugin
3)sarter-test
4)starter-actuator
5)starter-web


#)maven

Q)Can we use only Spring boot dependecy feature  and configure  maven plugin manually?
1)Yes
2)

Q)What is spring boot cli and what are its benfits?
comamnd tool to creat run and amange spring boot applicasiotn
-->Spring
-->spring help init
--> spring version
-->Spring init --dependecncies=web data-jpa my-project
-->spring init --list
->spring  init -d web cli-project


Q)What  is Thymeleaf?
java -based server side  templating engine used in java web applications to
render dynamic web pages.
In order to use dynamic  template html then we use themleaf.


-------------------------------------------------
. What is @Entity?
Represents a database table  


-------------------------

@NotNull

✔ Field must not be null
✘ Empty is allowed
✘ Blank string is allowed

Example:
@NotNull
private String name; // null ❌, "" ✔, " " ✔

2️⃣ @NotEmpty

✔ Field cannot be null or empty ("")
✘ Blank spaces (" ") are allowed

Example:
@NotEmpty
private String address; // null ❌, "" ❌, " " ✔

3️⃣ @NotBlank

✔ Field cannot be null, empty, or only blank spaces
💡 Most commonly used for Strings

Example:
@NotBlank
private String username; // null ❌, "" ❌, "   " ❌

4️⃣ @Size(min, max)

✔ Validates length of String, array, list, collection
💡 Often used with @NotBlank

Example:
@Size(min = 3, max = 10)
private String password; 
-----------------------------

5️⃣ @Email

✔ Validates correct email format
💡 Optionally use with @NotBlank or @NotEmpty

Example:
@Email
@NotBlank
private String email;

------------------
What is @Id?
Primary key field.

-------------------------------------------------------
23. What is @GeneratedValue?
Auto-increment primary key.
--------------------------------

How to write custom query?
@Query("select u from User u where u.email=?1")

-----------------------------------------

What is Spring Initializer?
Spring Initializer is a web application that helps you to create an initial 
spring boot project structure and provides a maven

---------------------------------------------------------------------
#)Hibernate is a ORM (Object Relational Mapping) framework in Java.
It helps you convert Java objects ↔ Database tables automatically.

#)Hibernate removes the need to write raw SQL queries for most CRUD operations.
You work with Java classes, and Hibernate handles the database.


#)Why is Hibernate used?
Because it makes database handling easier:
✔ No need to write SQL for CRUD
Hibernate auto-generates SQL like:
INSERT
UPDATE
SELECT
DELETE
--------------------------------------------------------------
Why do we use Hibernate in Spring Boot?

Because:

1️⃣ Spring Boot uses Hibernate by default

When you use Spring Data JPA, internally it uses Hibernate as the default ORM provider.

2️⃣ Saves development time

No SQL, no JDBC boilerplate, no connection management.

3️⃣ Integrates deeply with Spring Data JPA

JpaRepository

CrudRepository


-------------------------------

https://github.com/adarshdwivedi123/java_spring/tree/master


Interview experience read form here 

https://www.interviewbit.com/spring-boot-interview-questions/
https://www.naukri.com/code360/library/spring-boot-interview-questions


Java by happy
#)https://www.youtube.com/watch?v=NcsEXVO0Rtk&t=1783s

Spring boot by happy
https://www.youtube.com/watch?v=WxJi5yPuE3A



---------------------------------------
“H2 is an in-memory database mainly used for development and testing because it is fast
, embedded, and requires no setup.”

--------------------------------------------------------------------\
Modes of H2
1️⃣ In-Memory (Most common)

App band → data delete

Testing ke liye best









