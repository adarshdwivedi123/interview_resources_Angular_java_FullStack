
#)How Encapsulation Achieves Data Hiding
Encapsulation achieves data hiding using access modifiers (especially private) and getter/setter methods.

Let’s see an example 👇
✅ Example:
public class BankAccount {
    // Step 1️⃣: Make data private (hidden)
    private double balance;

    // Step 2️⃣: Provide public methods to access/modify it safely
    public void deposit(double amount) {
        if (amount > 0)
            balance += amount;
    }

    public void withdraw(double amount) {
        if (amount <= balance)
            balance -= amount;
        else
            System.out.println("Insufficient funds!");
    }

    public double getBalance() {
        return balance;
    }
}

🔍 Usage:
public class Main {
    public static void main(String[] args) {
        BankAccount account = new BankAccount();

        // ✅ Access via methods only
        account.deposit(5000);
        account.withdraw(2000);

        System.out.println("Current Balance: " + account.getBalance());

        // ❌ Direct access not allowed
        // account.balance = 100000; // ERROR — balance is private
    }
}


✅ Output:

Current Balance: 3000.0

🔒 What Happened Here

The balance field is private, so no one outside the class can access it directly.

Only the public methods (deposit, withdraw, getBalance) control how it’s modified.

This prevents unauthorized or invalid access.

That’s how Encapsulation achieves Data Hiding.


--------------------------------------------------------------------------------

💡 Real-Life Analogy

#)Think of a bank ATM machine 🏧
#)You can deposit or withdraw money using buttons (public methods).
#)You can’t directly access the internal account data or cash storage (private fields).
That’s Encapsulation + Data Hiding in real life.










------------------------------------------------------------------------------------


Type of casting
Widening: low to high type casting( Implict typr casting)
Narrowing: High to low type casting(explicit typr casting)
isme hme type mention krna pde  ga otherwise data loss ho jaye ga

int l=24
int f= l(data losss we neeed to mention type)case
int f=(int)l;


In java memory take 4 byte
int and float  becoz memoy repressantion  alg hoa hai







