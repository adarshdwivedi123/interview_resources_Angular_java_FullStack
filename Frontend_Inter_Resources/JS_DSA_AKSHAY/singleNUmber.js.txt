arr=[2,2,5,6];
missing={};
for(let i=0 ;i<arr.length;i++)
{
    if(!missing[arr[i]])
    {
       missing[arr[i]]=1;
       
    }
    else{
         missing[arr[i]]++;
    }
    
}
for(let i=0;i<arr.length;i++)
{
    if( missing[arr[i]] == 1)
    {
     console.log(arr[i]);   
    }
    
    
}
