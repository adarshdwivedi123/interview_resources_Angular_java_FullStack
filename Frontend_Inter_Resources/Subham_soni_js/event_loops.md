The Event Loop in JavaScript is what allows JavaScript to handle asynchronous operations (like API calls, timers, promises) even though JavaScript is single-threaded.

1)JavaScript is Single Threaded
JavaScript can execute only one task at a time.


2️⃣ Components of Event Loop

There are 3 main parts:
1️⃣ Call Stack
2️⃣ Web APIs
3️⃣ Callback Queue / Microtask Queuec