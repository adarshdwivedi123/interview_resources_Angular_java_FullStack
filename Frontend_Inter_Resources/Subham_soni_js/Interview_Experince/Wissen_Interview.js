
ngOnInit(): void {
    this.setForm();
    // this.searchText();
  }
	
  arr: any = ['ram', 'sham', 'ada', 'rishabh'];
  // setForm() {
  //   this.formGroup = this.formbuilder.group({
  //     searchData: [''],
  //   });
  }

  // searchText() {
  //   let searchList = this.formGroup.get('searchData').value;
  //   clearTimeout: any;
  //   setTimeout(() => {
	// 		searchList.filter((item , index ,arr)=>{
  //       return  arr.indexOf(item) === index ;
	// 	})
	// 	clearTimeout();

	// 	}, 2000);(*)
  // }
	 api1="http://";
	 ap2="htt"

	 getMthod()
	 let data=ForkJoin(api)
	    data.pipe().from(()=>{
				
			})
	 let data =api
	   return this.http.get(data,{Headeer:Data});



		 obj1 ={
           firstNAme:
					 lastname:
					 address:{
						 lien 
						 line:{

						 } 
						 line 
					 }
		 }

}
differmce between  two objet are same?
ob1={

};

1. Lodash isEqual — the standard answer
javascriptimport { isEqual } from 'lodash';

isEqual({ x: 1, y: 2 }, { x: 1, y: 2 });           // true
isEqual({ a: 1, b: 2 }, { b: 2, a: 1 });           // true — key order doesn't matter
isEqual({ a: { b: { c: 1 } } }, { a: { b: { c: 1 } } });  // true — handles nesting
isEqual([1, 2, 3], [1, 2, 3]); 
----------------------------------------------------------------------------

JSON.stringify — quick and dirty
javascriptJSON.stringify(a) === JSON.stringify(b);  // true if contents match
Works for simple plain objects. But it breaks when:

Keys are in different order — {a:1,b:2} vs {b:2,a:1} → false even though equal

// arr.reduce()

//Debouucing impemtaiom in search  bar
// 


How to add css globally
hwo to add css dynamically



/////////////////////////////////
#)ROuterLInk and ROuter.navigate wat is the differnece in between that
#)HOw to sett Error Globally  ?
#)what are differn ways we can do that
#)Network Protocal
#)if we have multiple api and getting the error how can we 




