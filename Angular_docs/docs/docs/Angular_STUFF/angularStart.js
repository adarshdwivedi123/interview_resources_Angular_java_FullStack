Angular 17 starts the app from main.ts.
It loads the main AppComponent using bootstrapApplication.
Then it replaces the <app-root> tag in the HTML with the component’s content.
After that, routing loads other components when needed.
This is how the app starts.